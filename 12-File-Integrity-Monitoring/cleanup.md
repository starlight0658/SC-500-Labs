# Cleanup — Lab 12

- Remove the temporary `sc500-fim-test.conf` test file if it is no longer needed.
- Stop/deallocate `vm-sc500-linux` after completing remaining labs.
- If Defender for Servers / FIM was enabled only for the bootcamp, disable paid coverage after all required evidence is captured.
- Do not delete Log Analytics evidence until the portfolio screenshots and notes are complete.
