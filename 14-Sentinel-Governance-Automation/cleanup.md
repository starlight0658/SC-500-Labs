# Cleanup — Lab 14

After portfolio evidence is captured:

- Restore the diagnostic setting to the desired log categories.
- Disable the scheduled analytics rule if ongoing lab alerts are not needed.
- Disable or delete the automation rule if the lab will not be used again.
- Delete the `SC500-Governance-Incident-Review` Logic App if it is no longer required, especially to avoid unnecessary paid executions/resources.
- Remove temporary playbook permissions if the resource group will no longer host Sentinel automation.
- Keep the screenshots, KQL, and documentation as the permanent evidence package.
