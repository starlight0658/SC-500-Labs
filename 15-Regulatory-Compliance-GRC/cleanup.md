# Cleanup — Lab 15

After portfolio evidence is captured:

- Keep the `sc500-compliance-logging` diagnostic setting if continued centralized workflow logging is desired.
- Keep `LogicAppsManagement` enabled while Consumption Logic App monitoring remains required.
- Disable or delete lab-only Logic Apps that are no longer needed to reduce ongoing cost and additional compliance findings.
- Review the remaining Logic App recommendation and either enable diagnostic logging or remove the lab resource if it is no longer required.
- Disable lab-only Sentinel analytics/automation from earlier modules if continued alerts are not needed.
- Retain screenshots and documentation as the permanent GRC evidence package.
